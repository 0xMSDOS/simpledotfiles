#!/bin/sh 

calendar () {
  FILE="$HOME/.cache/calendar.file"

  if [[ ! -f "$FILE" ]]; then
    touch "$FILE"
    eww -c $HOME/.config/bspwm/rice/moran/ open popHour
  else
    eww -c $HOME/.config/bspwm/rice/moran close popHour
    rm "$FILE"
  fi 

}

calendar
